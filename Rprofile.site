local({
    r <- getOption("repos")
    r["CRAN"] <- "https://packagemanager.rstudio.com/cran/__linux__/focal/2021-07-06"
    options(repos = r)
    options(HTTPUserAgent = sprintf("R/%s R (%s)", getRversion(), paste(getRversion(), R.version$platform, R.version$arch, R.version$os)))
})